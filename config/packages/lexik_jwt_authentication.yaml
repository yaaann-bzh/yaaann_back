lexik_jwt_authentication:
    secret_key: '%env(resolve:JWT_SECRET_KEY)%'
    public_key: '%env(resolve:JWT_PUBLIC_KEY)%'
    pass_phrase: '%env(resolve:JWT_PASSPHRASE)%'
    token_ttl:        1800
    user_identity_field:  email

# BEWARE check privilages for reading and writing to files generated by openssl !!!

# regenerate keys, prefer use :
# $ openssl genrsa -out config/jwt/private.pem -aes256 512
# $ openssl rsa -pubout -in config/jwt/private.pem -out config/jwt/public.pem 

# use the next sentence into vhost on apache2:
# SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1