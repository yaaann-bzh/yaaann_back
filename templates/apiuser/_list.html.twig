<div class="row">
    <div class="col-12 col-md-8 offset-md-2">
        <h2 class="h3">Liste des clés API déjà existantes</h2>
        {% for message in app.flashes('notice') %}
            <p class="alert alert-info" role="alert">
                {{ message }}
            </p>                
        {% endfor %}

        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">id</th>
                    <th scope="col">Application</th>
                    <th scope="col">Email</th>
                    <th scope="col">Date de création</th>
                </tr>
            </thead>
            <tbody>
            {% for apiuser in apiusers %}
                <tr>
                    <th scope="row"><a href="{{ path('show_apiuser', { 'id': apiuser.id}, relative = true) }}">{{ apiuser.id }}</a></th>
                    <td>{{ apiuser.appName }}</td>
                    <td>{{ apiuser.email }}</td>
                    <td>{{ apiuser.creationDate | format_datetime(pattern="dd-MM-Y") }}</td>
                    <td><a href="{{ path('delete_apiuser', { 'id': apiuser.id}, relative = true) }}">supprimer</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {% if apiusers is empty %}
            <p class="alert alert-info" role="alert">
                Aucune clé à afficher !
            </p>    
        {% endif %}
    </div>      
</div>
