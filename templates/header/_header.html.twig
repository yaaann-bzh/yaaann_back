<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">Yaaann Back-end</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample04">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('add_apikey') }}">Clés API</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('add_user') }}">Utilisateurs</a>
                </li>
            </ul>
            {% if app.user is defined %}
                <ul class="navbar-nav my-2 my-md-0">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('security_logout') }}">Se déconnecter</a>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>